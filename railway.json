{
    "build": {
      "env": {
        "PYTHONUNBUFFERED": "1"
      },
      "command": "pip install -r backend/requirements.txt",
      "cwd": "backend"
    },
    "deploy": {
      "command": "uvicorn main:app --host 0.0.0.0 --port $PORT",
      "cwd": "backend"
    }
  }
  